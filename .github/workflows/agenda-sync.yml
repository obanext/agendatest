name: OBA agenda sync

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel sync
        run: |
          curl -fsS https://agendatest.vercel.app/api/sync
